<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.memories.mapper.MemoriesMapper">

    <select id="findId" parameterType="java.lang.String" resultType="cc.mrbird.febs.memories.entity.Memories">

    select * from memories
        where id=#{id}
    </select>
</mapper>
